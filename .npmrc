# M9D 001-PQPRIM: `engines` is advisory unless this is set. ML-KEM and ML-DSA exist in node:crypto
# only from Node 24.7, so an install on 24.0–24.6 must fail here, not at the first signature.
engine-strict=true
