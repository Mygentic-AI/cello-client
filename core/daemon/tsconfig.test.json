{
  // DOD-M12B-TESTS-NOT-TYPECHECKED-1 — a FOOTHOLD, deliberately not the whole thing.
  //
  // Every package excludes `src/__tests__` from `tsc --build`, so a test asserting on a field that
  // does not exist, or calling a method that was removed, is invisible to the gate — it fails only
  // if vitest happens to execute that line. That is the hollow-test shape every review in this
  // milestone has hunted, institutionalised. It already let a defect ship: `msg-018` called
  // `ensureStandingReceiverForAgent` with two arguments against a one-parameter signature and
  // `pnpm run typecheck` reported exit 0.
  //
  // Switching it on repo-wide surfaces **339** errors in the daemon's existing tests alone — mostly
  // deliberate partial fakes that need `as unknown as` casts, which is a real cleanup and not this
  // unit. So this covers the files added by the interrupted-session work plus the shared helpers,
  // which is where the defect shipped and where a regression would land next. Widening it is
  // `DOD-M12B-TESTS-NOT-TYPECHECKED-1`; add files here as they are cleaned.
  //
  // `files` rather than `include`, because `exclude` in the base config filters `include` and would
  // drop every one of these.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "src"
  ],
  "files": [
    // `DOD-M15-SEALWIRE-1` B2b-1 pass-2 F3. Both are NEW and clean, which is this list's stated
    // admission criterion — and both contained a real type error the gate could not see: they
    // passed `undefined` for `leafKind`, which is now `number`, and it was silently rebuilt one hop
    // below by `submitMessageHash`'s own default. A test file outside this allowlist is a file where
    // "the mutants are typecheck failures now" is not true.
    // Added on creation, per the trap in RESUME STATE: a test file outside this list is one where
    // "the mutants are typecheck failures now" is not true.
    // DOD-M15-RELAYONLY-1. New and clean on creation, which is this list's admission criterion —
    // and a privacy control is exactly the kind of unit where "the mutants are typecheck failures
    // now" needs to be true rather than assumed.
    "src/__tests__/dod-m15-bootstrap-auth-1-poisoned-coordinate.test.ts",
    "src/__tests__/dod-m15-relayleak-1.test.ts",
    "src/__tests__/dod-m15-relayonly-1.test.ts",
    "src/__tests__/dod-m15-salt-adoption-rule.test.ts",
    "src/__tests__/dod-m15-salt-capability-signal.test.ts",
    "src/__tests__/dod-m15-send-consults-the-salt.test.ts",
    "src/__tests__/dod-m15-salt-visible-to-the-agent.test.ts",
    "src/__tests__/dod-m15-send-names-its-algorithm.test.ts",
    "src/__tests__/dod-m15-retry-queue-column-upgrade.test.ts",
    "src/__tests__/msg-020-revival-bound.test.ts",
    "src/__tests__/msg-021-session-seed.test.ts",
    "src/__tests__/msg-022-session-rebuild.test.ts",
    "src/__tests__/msg-023-revive-on-send.test.ts",
    "src/__tests__/msg-024-revive-on-read.test.ts",
    "src/__tests__/msg-025-pending-seal-resolve.test.ts",
    "src/__tests__/msg-026-reap-held.test.ts",
    "src/__tests__/msg-027-revive-reservation.test.ts",
    "src/__tests__/msg-028-relay-dispatch-guidance.test.ts",
    "src/__tests__/msg-029-witnessed-leaf-fetch.test.ts",
    "src/__tests__/msg-013-restart-seal.test.ts",
    "src/__tests__/msg-014-restart-seal-wired.test.ts",
    "src/__tests__/msg-015-interrupted-escalate.test.ts",
    "src/__tests__/msg-016-sealed-status-lands.test.ts",
    "src/__tests__/msg-017-seal-waiter-key.test.ts",
    "src/__tests__/msg-018-reservation-retry.test.ts",
    // DOD-M15-SALTSPLIT-1 / SEALWIRE-1: these two were type-BROKEN and nobody could see it.
    // `placeOwnLeaf` lost its `kind` and `authorship` defaults so a caller must state both, and nine
    // calls across these two files were left invalid — five passing six arguments, four passing five.
    // They compiled only because this allowlist did not include them, which is the allowlist doing
    // its job at the cost of hiding the very breakage the signature change was meant to make loud.
    // Added here because a required parameter that no compiler checks is a comment.
    "src/__tests__/msg-007-index-discipline.test.ts",
    /**
     * ⚠️ SEALWIRE-1's OWN FOUR FILES — missing until review found them, in the very commit that
     * widened this list for two OTHER units and wrote the trap down verbatim: *"the files most likely
     * to be missing are your own newest ones."* I wrote that sentence and then left bullet 5's
     * headline evidence outside the compiler.
     *
     * It was not free. `-sent-proof-wired` imported `Stream` from `@cello-protocol/transport`, which
     * does not export it — `error TS2305`. It is used three times as `as unknown as Stream`, so the
     * acking relay's stream shape was completely unconstrained in the file this bullet rests on. The
     * type came from `@libp2p/interface` all along; nothing caught it because nothing looked.
     */
    "src/__tests__/dod-m15-sealwire-1-authorship.test.ts",
    "src/__tests__/dod-m15-sealwire-1-root-check.test.ts",
    "src/__tests__/dod-m15-sealwire-1-sender-leg.test.ts",
    "src/__tests__/dod-m15-sealwire-1-sent-proof-wired.test.ts",
    "src/__tests__/dod-m15-sealwire-1-held-authorship.test.ts",
    "src/__tests__/dod-m15-sealwire-1-callsite-enforcer.test.ts",
    "src/__tests__/dod-m15-saltannounce-late-1.test.ts",
    "src/__tests__/relay-client-fake.ts",
    // DOD-M15-SALTSPLIT-1's own two files. New and clean on creation, which is this list's stated
    // admission criterion — and I nearly shipped them OUTSIDE it in the same session I widened the
    // list for someone else's breakage. That is the whole trap in one move: the allowlist protects
    // what you remember to add, so the files most likely to be missing are your own newest ones.
    "src/__tests__/dod-m15-salt-peer-closed-convergence.test.ts",
    "src/__tests__/dod-m15-saltsplit-1-closed-is-not-empty.test.ts",
    // DOD-M15-RELAYSLOTS-1's own file. New and clean on creation, which is this list's admission
    // criterion — and the trap written twice above caught me on the way in: I ran the root typecheck
    // against a file calling a method that did not exist yet and it reported exit 0, then a
    // deliberate `const x: number = "not a number"` in the same file ALSO reported exit 0. Without
    // that positive control I would have read a green gate as evidence the API was already there.
    "src/__tests__/dod-m15-relayslots-1-online-token-carried.test.ts",
    "src/__tests__/dod-m15-relayslots-1-refusal-surfaced.test.ts",
    // DOD-M15-NO-SILENT-REFUSAL-1's own file. New and clean on creation, which is this list's
    // admission criterion — and its predecessor is the argument for it: two calls in
    // `dod-m15-refused-inbound-silent-1.test.ts` omit `takeContentRefusals`' REQUIRED third
    // argument, whose own docstring says the protection is being unable to forget it. They ran for
    // weeks because `undefined` is a usable Map key and no compiler reads that file.
    "src/__tests__/dod-m15-no-silent-refusal-1.test.ts",
    // ⚠️ `m12-p14-seal-readiness.test.ts` is NOT here, and the omission is measured rather than
    // assumed. Its nine-argument `placeOwnLeaf` calls are fixed in the same commit, but adding the
    // file surfaces FIVE pre-existing errors that have nothing to do with this unit: four
    // `implicitly has an 'any' type` on a filter callback, and one real one at line 363 —
    // `Argument of type '"peer_gone"' is not assignable to parameter of type
    // '"sealed" | "error" | "interrupted"'`, a test passing a status the function cannot take.
    // That last one is a genuine finding and it belongs to `DOD-M12B-TESTS-NOT-TYPECHECKED-1`, not
    // here. Fixing someone else's file to widen my own guard is how a unit stops being reviewable.
    //
    // ⚠️ AND SAY THE COST OUT LOUD — review MEDIUM-3. The sentence above read as though the file is
    // now fine. It is not: those four calls are **UNVERIFIED BY ANY COMPILER**. No config reads this
    // file, so they are correct by inspection only, and the file is arguably in a worse state than
    // before — four calls that LOOK checked, in a file nothing checks. The five errors are mechanical
    // (two callback annotations, one string literal) for whoever closes that line.
    "src/__tests__/helpers/encrypted-db.ts",
    "src/__tests__/helpers/hold-singleton-lock.ts",
    "src/__tests__/helpers/park-relay.ts",
    "src/__tests__/helpers/seed-agents.ts",
    // `DOD-M15-SELFCHAIN-1`. New and clean on creation, which is this list's admission criterion.
    // It hands out the value every fixture's session chains to, so a signature drift here would
    // land silently in dozens of files at once — exactly the case for having the compiler see it.
    "src/__tests__/helpers/session-genesis.ts",
    "src/__tests__/helpers/spawn-real-daemon.ts",
    "src/__tests__/helpers/two-connection-fixture.ts"
  ]
}
