# core

The Node.js/TypeScript implementation of the CELLO protocol. Ships as an MCP server.

Handles all cryptography, prompt injection scanning, directory communication, and Merkle tree operations. Adapter implementations call into this — agent developers never touch it directly.

## Status

Pre-implementation.
