# core

The Node.js/TypeScript implementation of the CELLO protocol. Ships as an MCP server.

Implements the crypto primitives underlying the CELLO protocol — signing, verification, key management, Merkle tree operations — plus prompt injection scanning and directory communication. Adapter implementations call into this — agent developers never touch it directly.

## Status

Pre-implementation.
